import 'package:flutter/cupertino.dart';

base class BaseSampleStatelessWidget extends StatelessWidget{
  const BaseSampleStatelessWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}